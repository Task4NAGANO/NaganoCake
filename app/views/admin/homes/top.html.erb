<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <h3>注文履歴一覧</h3>
      <table class="table">
        <thead>
          <tr>
            <th>購入日時</th>
            <th>購入者</th>
            <th>注文個数</th>
            <th>注文ステータス</th>
          </tr>
        </thead>
        <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td>
              <%= link_to admin_oder_path(order.id) do %>
                <%= order.created_at.strftime('%Y/%m/%d') %>
              <% end %>
            </td>
            <td>
              <%= order.customer.first_name %><%= order.customer.last_name %>
              </td>
            <td>
              <% total = 0 %>
              <% order.order_details.each do |order_detail| %>
                <% total += order_detail.amount %>
              <% end %>
              <%= total %>
            </td>
            <td>
              <%= order.order_status %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>