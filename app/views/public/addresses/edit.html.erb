<div class="container">
  <div class="row">
  <h3>配送先編集</h3>
    <div class="col-sm-12 col-md-7 col-lg-7 px-1 px-sm-0 mx-auto">
      <%= form_with model: @address do |f| %>
      <teble class="address">
        <tbody>
          <tr>
            <td><%= f.label "郵便番号(ハイフンなし)" %></td>
            <td><%= f.text_field :postal_code %></td>
          </tr>
          <tr>
            <td><%= f.label "住所" %></td>
            <td><%= f.text_area :address %></td>
          </tr>
          <tr>
            <td><%= f.label "宛名" %></td>
            <td><%= f.text_field :name %></td>
            <td><%= f.submit "変更内容を保存", class: "btn btn-success" %></td>
          </tr>
        </tbody>
      </teble>
      <% end %>
    </div>
  </div>
</div>